#include <iostream>
#include "barang.h"
#include <stdio.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	BARANG B;
	char *a;
	//a = "a";
	
	CreateBarang(a,10);
	TampilBarang(B);
	printf("tes");
	return 0;
}
